# Deprecation Warning
Those are the old environment descriptors, they don't work with the TUI.

This part has not been deleted yet since the CLI interface still uses said files.

Those files will be deleted as soon as the CLI/TUI interfaces' back-end will be unified.
