{
  "ring_nccl": {
    "desc": "NCCL ring algorithm for Reduce: a simple peer‑to‑peer pipeline ring.",
    "version": "2.5",
    "selection": "ring",
    "tags": [
      "ring",
      "reduce"
    ]
  }
}
